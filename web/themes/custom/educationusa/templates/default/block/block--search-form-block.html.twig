{% extends "@bootstrap_barrio/block/block.html.twig" %}
{#
/**
 * @file
 * Bartik's theme implementation for a search form block. Extends Barrio's
 * search form block template.
 *
 * Available variables:
 * - content: The content of this block.
 * - content_attributes: A list of HTML attributes applied to the main content
 *   tag that appears in the template.
 *
 * @see template_preprocess_block()
 * @see search_preprocess_block()
 *
 * @ingroup themeable
 */
#}
{% block content %}
  <!-- Hiding Search Box and Search Button. Replacing both with image of Magnifying Glass-->
  <div{{ content_attributes.addClass('content', 'container-inline') }} style="display:none;">
    {{ parent() }}
  </div>
 <a href="/search" border=0><button class="exchangealumni-search-submit" id="Search-magnifying-glass"></button></a>
{% endblock %}
